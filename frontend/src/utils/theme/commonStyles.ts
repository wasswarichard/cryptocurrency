import { makeStyles } from '@mui/styles';

const useCommonStyles = makeStyles(() => ({
   mainContainer: {
      display: 'flex',
   },
   paddingTopBottom30: {
      padding: '30px 0',
   },
}));

export default useCommonStyles;
